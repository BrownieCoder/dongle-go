# 独立工程师 3 审查

日期：2026-09-20。审查者未参与产品实现；仅写入本报告。初审对象是当前工作区源码，不是已发布提交。软件预览审查和硬件交付验收分开，尚无真机通过证据。

**最终软件复审：96/100，通过软件预览审查；硬件交付仍未通过。** 下方保留初审和修复记录，最终结论及证据见末节。

## 初审结论

**87/100，暂未达到 95 分门槛；硬件交付未通过。** 没有在本审查范围发现未解决的 P0/P1。现有功能准确定位为只读诊断和模拟配置预览，不能据此称为成熟、真机可用 MVP。

| 软件评分项 | 分数 | 依据与扣分 |
|---|---:|---|
| 产品正确性 | 29/30 | 空固件白名单阻止真实配置；原配置持久化、身份匹配、进程间锁及重读有测试。检查成功和联网成功明确区分。完整 USB/串口与失败边界另由专项审查覆盖。 |
| 易用与双语 | 22/25 | UI 和教程均有中英文，目标设备区别清楚；错误映射对损坏快照、备份保存失败不准确；发现文案仍仅描述串口。 |
| 分发可靠 | 19/25 | 固定依赖、原生两平台构建 CI、许可证与 libusb 对应源码齐备；没有已运行打包产物或 Windows CI 证据，启动 smoke 尚待完成。 |
| 测试与文档 | 17/20 | 本地核心、双语、真实 Tk 测试通过；英文教程图片尚缺、资产文档链接缺失、文档检查范围不全、验收文档没有明确本次软件量表。 |
| 合计 | **87/100** | 必须复审修复后的工作区和产物。 |

## 发现与交付缺口

1. **P2：恢复错误误导。** `error_key` 的子串匹配将 `invalid_snapshot` 和 `snapshot_failed` 均归为“本次会话没有恢复记录”，无法区分损坏备份和保存失败。`identity_mismatch`、`recovery_pending`、`read_only_transport` 也只提示通用重试。底层拒绝危险写入，但用户缺少正确下一步。已反馈实现者；需精确映射和回归测试。
2. **P2：发现提示未同步 USB 诊断。** 空列表中英文仍称“未找到支持的串口连接”，与已实现的 USB 发现不一致。已反馈实现者。
3. **分发证据不足。** CI 构建文件正确地只上传预览 artifact，不发布 release；但尚无冻结程序启动验证。需要有界、无硬件 smoke，确认 Tk 与打包 libusb 可加载，并在目标系统运行。macOS 本地通过也不能当作 Windows 通过。
4. **文档检查遗漏。** `scripts/check_docs.py` 不扫描 `THIRD_PARTY_NOTICES.md` 和 `licenses/README.md`，漏掉前者指向尚不存在的 `docs/assets.md`。英文图片正在生成，初审时 README 图链确实不存在。
5. **量表区分不足。** 初审时 `docs/acceptance.md` 仅有包含真机证据的正式交付量表，不能将它与本报告的软件 30/25/25/20 量表混算。需明确两种结论。

恢复按钮根据全局快照存在启用，未在 UI 先核对所选设备；底层仍会核对，故本次不将其判为误写问题。错误提示应清晰说明设备不匹配。

## 直接验证证据

- `.venv-tk/bin/python -m unittest discover -s tests -p 'test_core.py' -v`：20 项通过。
- `.venv-tk/bin/python -m unittest discover -s tests -p 'test_i18n.py' -v`：3 项通过。
- `.venv-tk/bin/python -m unittest discover -s tests -p 'test_app.py' -v`：4 项通过。通过已授权的 macOS 窗口服务运行真实 Tk，所有服务均为无硬件模拟。
- `.venv-tk/bin/python scripts/check_docs.py`：失败，缺 `assets/tutorial-en.png`。
- 只读检查了 README、双语教程与排障、兼容性/真机验收文档、许可证清单、构建脚本、CI、UI、国际化、核心状态流。

## 已确认的边界

两份 README 明确预览无真机验证，不建议据此购买设备。iPad 是最终使用端，配置仍需 Mac/Windows；未把目标选择或桌面配置成功说成 iPad 已联网。没有驱动自动替换、固件刷写或假报互联网验证。后台工作通过队列回到 Tk 主线程；忙时禁用操作，目标改变使检查结果失效，退出等待当前操作完成，意外错误不会显示原始设备输出。

许可证包含对应源码和版本差异提醒，但这不是实际二进制依赖清单审计。Windows 清洁系统、真实新手使用、双平台包运行、硬件恢复和真实移动网络出口均尚无通过证据。

## 复审

复审工作区对应提交：`296a0460ea465416d6b41e76313047dfd8c144c7`。复审确认前述恢复错误、USB 发现提示、文档扫描范围、英文图片缺失与量表区分问题均已修复。

- `ERROR_KEYS` 明确覆盖核心错误码；新增 AST 测试检查遗漏。直接复跑双语测试 4 项通过。
- 直接复跑文档检查通过，已覆盖根目录和许可证目录的 Markdown。
- 独立查看中英文两张九宫格，文字可读、九步完整，底部明确开发预览/未知固件只读，第八步要求在目标设备实际验证。插画明确是外形示意，不能作为型号识别证据。
- 检查 `scripts/smoke_bundle.py`：解压真正的 ZIP，再启动其中可执行程序；45 秒外部超时，内部 15 秒超时；不枚举硬件。读取本地 `dist/smoke-report.json`，8 项通过。此报告由实现者执行产生，审查者复核代码和结果，未把它宣称为自身独立执行。
- CI 已配置同一产物 smoke，上传报告、ZIP 和校验文件。目标平台实际运行结果仍等待确认。

**中间复审：94/100 = 产品正确性 29 + 易用与双语 24 + 分发可靠 22 + 测试与文档 19。** 两个 P2 已关闭，尚未给予待运行的 Windows/macOS CI 证据分。软件未完成签核；真机验收继续为未通过。用户安排硬件到货后二次验收，不降低该门槛。

## 最终软件复审与签核

独立通过 `gh run view 35503009255 --repo BrownieCoder/dongle-go --json headSha,status,conclusion,jobs,url` 核实公开 CI，确认运行完成且结论为 `success`，绑定提交 `296a0460ea465416d6b41e76313047dfd8c144c7`。

- [Windows job 106058002047](https://github.com/BrownieCoder/dongle-go/actions/runs/35503009255/job/106058002047)：原生桌面测试、文档检查、构建、从 ZIP 解压启动模拟 smoke、产物与校验文件上传全部成功。
- [macOS job 106058002074](https://github.com/BrownieCoder/dongle-go/actions/runs/35503009255/job/106058002074)：相同原生测试及产物运行步骤全部成功。
- [Linux job 106058002012](https://github.com/BrownieCoder/dongle-go/actions/runs/35503009255/job/106058002012)：虚拟显示下测试与文档检查成功；未构建 Linux 分发包，没有扩大产品平台支持声明。

| 最终软件评分项 | 分数 | 保留扣分与边界 |
|---|---:|---|
| 产品正确性 | 29/30 | 当前软件预览状态流与失效保护证据充分；模拟不是所有 OS 异常与实际设备行为的穷举。 |
| 易用与双语 | 24/25 | 初审提示问题关闭、双语资产完成；尚无独立新手试用/辅助功能评估。 |
| 分发可靠 | 24/25 | 三平台 CI、两平台实际分发 ZIP 启动证据已补；未做签名/公证及消费者干净桌面安装体验验收。 |
| 测试与文档 | 19/20 | 当前自动化与文档检查通过；真实用户和硬件场景的证据仍须补齐。 |
| 合计 | **96/100** | **软件开发预览通过；无未解决 P0/P1，初审两个 P2 已关闭。** |

这里的分发通过指 CI 生成的开发预览 ZIP 可以在其对应 runner 解压并启动，无硬件模拟完成。Windows Server runner 不能替代 Windows 11 干净系统驱动与新手验收，macOS CI 不能证明物理模块兼容。许可证资料审查也不替代正式二进制发布的实际组件和 LGPL 替换能力审计。

**本签核不宣称成熟 MVP 已交付，不开放任何未经真机验证的固件写入，不给硬件验收补分。** 后续仅修改 README 下载入口、审核及验证证据时，应检查文档一致性；任何产品代码、依赖、构建/运行逻辑变化都需要与变化相称的重新验证。
